# This is a Databricks asset bundle definition for TestBundle3.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: TestBundle3
  uuid: 8238c4aa-8794-4a88-8a56-5aff26d38409

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-370f7e99-0261.cloud.databricks.com

  prod:
    mode: production
    workspace:
      host: https://dbc-370f7e99-0261.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/ih.kii777@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/ih.kii777@gmail.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: ih.kii777@gmail.com
        level: CAN_MANAGE
    variables:
      target_catalog_bronze: ${var.catalogue_bronze_name_prod}
      target_database_bronze: ${var.database_bronze_name_prod}
    resources:
      jobs:
        ETLRunner:
          git_source:
            git_url: https://github.com/ihor123/databricksLessons20250715.git
            git_provider: gitHub
            git_branch: main


